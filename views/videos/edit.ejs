<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <title>Edit Video</title>
</head>
<body>
    <h1>Edit <%=video.title%> </h1>
    <form action="/videos/<%=video._id%>?_method=PUT" method="POST" class="form"> <!-- trick EJS into doing a PUT by disguising as POST -->
        <label for="title">Video Title: </label>
        <input name="title" type="text" value="<%=video.title%>"/>
        <label for="channel">Channel: </label>
        <input name="channel" type="text" value="<%=video.channel%>"/>
        <label for="link">Video Link: </label>
        <input name="link" type="text" value="<%=video.link%>"/>
        <label for="favorite">Favorite?</label>
        <input
            name="favorite"
            type="checkbox"
            <% if (video.favorite) { %>
                checked
            <% } %> />
        <button type="submit" class="buttonText">Update Video</button>
    </form>
    <button class="buttonText">
        <a href="/videos">Back to Video Library</a>
    </button>
</body>
</html>